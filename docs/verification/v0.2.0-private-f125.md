# v0.2.0 private F1 25 validation

This record stores only the commit-safe conclusion from the local F1 25 validation assistant. Raw
telemetry and detailed diagnostics stay under the owner's local application-data directory and must
not be copied into the repository.

## Owner checks

- [x] Run `scripts/ValidatePrivateF125.ps1` from a clean tracked worktree.
- [x] Complete the offline Time Trial probe and exactly one native ApexLab capture.
- [x] Confirm the command reaches `safeSummary` and prints `PASS`.
- [x] Review the generated record against the 14-field commit-safe schema before copying it here.

Clean-machine packaging and browser-origin SmartScreen behavior are distribution gates retained for
the pre-v1.0 milestone. They are not inferred from this private telemetry validation.

## Commit-safe result

The owner completed the guided real-game workflow on 2026-08-09. The exact privacy-safe contents of
`%LOCALAPPDATA%\ApexLab\private-validation\latest-safe.json` are recorded below:

```json
{"schemaVersion":1,"status":"passed","validationDate":"2026-08-09","gameBuild":"F1 25 current PC build","adapterId":"ea-f1-25-v3","applicationVersion":"0.1.0","offlineCapture":true,"manifestIntegrity":true,"deterministicReplay":true,"sequenceGapPreservation":true,"zeroPrivacyExcludedEvidence":true,"probeAssumptions":true,"rateGate2x":true,"conclusion":"PASS"}
```

Allowed fields are `schemaVersion`, `status`, `validationDate`, `gameBuild`, `adapterId`,
`applicationVersion`, `offlineCapture`, `manifestIntegrity`, `deterministicReplay`,
`sequenceGapPreservation`, `zeroPrivacyExcludedEvidence`, `probeAssumptions`, `rateGate2x`, and
`conclusion`.

`zeroPrivacyExcludedEvidence` means privacy-excluded packet classes were not retained in finalized
evidence. It does not claim that the live UDP stream contained no excluded packet classes.
