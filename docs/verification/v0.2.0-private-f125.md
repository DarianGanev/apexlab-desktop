# v0.2.0 private F1 25 validation

This record stores only the commit-safe conclusion from the local F1 25 validation assistant. Raw
telemetry and detailed diagnostics stay under the owner's local application-data directory and must
not be copied into the repository.

## Owner checks

- [ ] Run `scripts/ValidatePrivateF125.ps1` from a clean feature-branch worktree.
- [ ] Complete the offline Time Trial probe and exactly one native ApexLab capture.
- [ ] Confirm the command reaches `safeSummary` and prints `PASS`.
- [ ] Review the portable Windows package on a clean Windows account, including SmartScreen
      behavior for the unsigned build.

## Commit-safe result

After a successful real run, copy only the exact contents of
`%LOCALAPPDATA%\ApexLab\private-validation\latest-safe.json` into a fenced JSON block below. Before
that reviewed owner action, this section intentionally contains no completed JSON result.

Allowed fields are `schemaVersion`, `status`, `validationDate`, `gameBuild`, `adapterId`,
`applicationVersion`, `offlineCapture`, `manifestIntegrity`, `deterministicReplay`,
`sequenceGapPreservation`, `zeroPrivacyExcludedEvidence`, `probeAssumptions`, `rateGate2x`, and
`conclusion`.
