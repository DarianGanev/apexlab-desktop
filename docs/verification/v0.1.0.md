# ApexLab v0.1.0 verification record

This record tracks the Windows desktop foundation release candidate. Pending items are stated
explicitly; they are not treated as completed evidence.

## Candidate environment

| Item | Value |
| --- | --- |
| Verification date | 2026-07-20 |
| Operating system | Windows 11 x64 |
| SDK | .NET SDK 10.0.302 (pinned by `global.json`) |
| Runtime package | Self-contained `win-x64` ZIP |
| Source branch | `release/0.1.0` from verified `develop` commit `5139272` |
| Release tag | **Pending release** |
| Integration CI | [develop merge run 29767877016](https://github.com/DarianGanev/apexlab-desktop/actions/runs/29767877016) |
| Release-candidate CI | **Pending release-branch push** |

## Automated evidence

The following commands pass locally from a clean repository worktree:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/Verify.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/Publish.ps1 -Version 0.1.0 -CleanKnownOutputs
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/SmokeTest.ps1 -PackagePath artifacts/ApexLab-0.1.0-win-x64.zip
```

Current integration evidence:

- locked restore, format verification, and Release build pass with 0 warnings and 0 errors;
- 177 discovered tests pass: 26 Application, 41 Persistence, and 110 Integration;
- TRX and Cobertura coverage files are produced under `artifacts/test-results`;
- no numeric coverage gate is claimed for the foundation release;
- repository checks reject conflict markers, AWS/GitHub/private-key credential shapes, personal-data
  directories, oversized synthetic fixtures, and generated source-tree changes;
- every repository check proves its failure path with an intentionally bad temporary fixture;
- runtime-specific `win-x64` NuGet graphs are locked;
- a second publish without `-CleanKnownOutputs` is rejected rather than overwritten;
- the ZIP checksum matches its SHA-256 manifest;
- the package contains `README.md` and `THIRD_PARTY_NOTICES.md` and does not claim a source license;
- the extracted executable exits 0 in headless smoke mode, writes validated settings, initializes
  SQLite schema version 1, reports version `0.1.0`, and leaves no running process or locked temp tree.
- startup-routing regressions prove smoke mode cannot invoke interactive shell construction, and smoke
  mode rejects existing caller data/result targets before mutation.

## Manual environment gates

- Clean Windows account without a development SDK: **not yet completed**. Windows Sandbox is not
  available and the release process has no permission to use the existing Hyper-V environment.
- Microsoft Defender SmartScreen: **not yet observed**. Locally produced files do not carry the
  browser-origin Mark-of-the-Web needed for a representative reputation check.

These are manual release gates, not automated-success claims.

## Release evidence to complete

- [ ] Record the exact release-candidate commit and successful CI URL.
- [x] Repeat verification on `release/0.1.0` from an isolated worktree: 177 tests, publish, and
  packaged smoke all passed on 2026-07-20.
- [ ] Confirm the package on a clean Windows account without a development SDK.
- [ ] Record unsigned binary and Microsoft Defender SmartScreen observations.
- [ ] Confirm the annotated `v0.1.0` tag and GitHub release assets.
- [ ] Record final package/checksum asset names; the checksum file remains the hash authority.

## Known limitations

- The package is unsigned and may trigger Windows reputation or SmartScreen warnings.
- Distribution is a portable self-contained ZIP, not an installer or auto-updater.
- The UI is the tested desktop shell; F1 25 UDP capture, replay, lap analysis, and coaching arrive in
  later milestones.
- Domain, protocol, and telemetry test assemblies are placeholders until those product slices begin.
- No public source-code license is granted by this package.
