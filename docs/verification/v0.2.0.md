# ApexLab v0.2.0 release verification record

This record consolidates the automated, synthetic, and private real-game evidence for the v0.2.0
capture-and-replay milestone. It contains no raw telemetry or private rate.

## Candidate environment

| Item | Value |
| --- | --- |
| Verification date | 2026-08-09 |
| Operating system | Windows x64 |
| SDK | .NET SDK 10.0.302 (pinned by `global.json`) |
| Runtime package | Self-contained `win-x64` ZIP |
| Release base | verified `develop` commit `8508c3df1eb8eb5ba36615c07db7ac076086c76d` |
| Source branch | `release/0.2.0` |
| Release tag | Pending final release |
| Release-candidate CI | Pending exact release-branch push |

## Required commands

Run from a clean `release/0.2.0` worktree:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/Verify.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/Publish.ps1 -Version 0.2.0 -CleanKnownOutputs
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/SmokeTest.ps1 -PackagePath artifacts/ApexLab-0.2.0-win-x64.zip
```

## Evidence available before the release branch

- The verified integration tree builds in Release with zero warnings and zero errors.
- 449 non-soak automated tests pass: 95 Application, 87 Persistence, 30 Protocols.F125, 28
  Telemetry, and 209 Integration tests.
- Fixed-seed adversity and loopback soak evidence covers bounded ingestion, exact accounting,
  corruption rejection, resource limits, lifecycle races, finalization, and deterministic replay.
- The private real-game workflow passed on 2026-08-09 using base F1 25 v3 in an offline Time Trial.
- The commit-safe private result confirms offline capture, manifest integrity, two matching replays,
  sequence-gap preservation, zero privacy-excluded finalized evidence, protocol assumptions, and an
  enforced rate gate of at least twice the privately measured real peak.
- Exact private rates, payloads, identifiers, hashes, paths, sender/session values, and screenshots
  remain outside Git.

Supporting records:

- [`v0.2.0-private-f125.md`](v0.2.0-private-f125.md): authoritative 14-field safe conclusion.
- [`v0.2.0-real-evidence.md`](v0.2.0-real-evidence.md): human-readable private gate conclusions.
- [`v0.2.0-synthetic.md`](v0.2.0-synthetic.md): automated adversity and rate-gate mechanics.

## Release evidence to complete

- [ ] Record the exact release-candidate commit and successful push/PR CI URLs.
- [ ] Confirm the `ApexLab-0.2.0-win-x64.zip` package and nested SHA-256 manifest from exact-head CI.
- [ ] Merge the release PR to `main`, rerun the package gate, and create annotated tag `v0.2.0`.
- [ ] Publish and redownload the GitHub release ZIP/checksum assets, then verify their nested hash.
- [ ] Merge the released state back to `develop` before starting v0.3.

## Distribution boundary

The portable package is unsigned and may trigger Microsoft Defender SmartScreen. No installer,
signing certificate, reputation guarantee, or auto-updater is claimed. Definitive clean-machine
self-contained and browser-origin Mark-of-the-Web observations remain the roadmap's pre-v1.0
distribution gate.

## Product boundary

v0.2.0 proves reliable base-F1-25 capture evidence and deterministic replay. It does not claim lap
reconstruction, corner analysis, driving findings, or coaching effectiveness; those begin in v0.3.
